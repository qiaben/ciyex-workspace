{
	"name": "ciyex-workspace-update-server",
	"version": "1.0.0",
	"private": true,
	"description": "Cloudflare Worker that serves Ciyex Workspace auto-updates from GitHub Releases",
	"main": "src/worker.ts",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"tail": "wrangler tail",
		"typecheck": "tsc --noEmit"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20250101.0",
		"typescript": "^5.6.0",
		"wrangler": "^3.95.0"
	}
}
