{
	"_comment": "Prod overlay. The base product.json is already prod-flavored (existing app names + AppIds preserved so prod auto-update upgrades existing installations in-place). Only updateUrl needs to change to point at the prod-latest pointer release.",
	"updateUrl": "https://github.com/qiaben/ciyex-workspace/releases/download/prod-latest/metadata.json"
}
